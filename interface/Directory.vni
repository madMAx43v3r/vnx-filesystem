package vnx.filesystem;

class Directory extends Entity {
	
	map<string, node_entry_t> nodes;
	
	bool is_public;						// if anybody can add files
	
	bool inherit_users;					// if to inherit users from parent
	
}
